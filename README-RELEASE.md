Release Readiness

- Ubuntu Linux
- macOS
- Windows

Auto-update layers explained:
- Layer 1: IDE shell auto-update (clawAutoUpdate)
- Layer 2: Claude CLI auto-update (clawCLIUpdate)
- Layer 3: (future) Clawd Bot auto-update

First-launch onboarding flow (clawOnboarding): welcome, locate claude, test, MCP config, ready

Build docs:
- Debian/Ubuntu: .deb and AppImage builds
- macOS: .dmg build
- Windows: .exe build
